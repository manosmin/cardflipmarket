{
  "dependencies": {
    "axios": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.5.3",
    "node-cron": "^3.0.3"
  },
  "type": "module",
  "scripts": {
    "server": "nodemon index.js",
    "build": "npm install --prefix ./client && npm run build --prefix ./client && npm install"
  }
}
